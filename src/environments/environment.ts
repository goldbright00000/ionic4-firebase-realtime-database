// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyAKSnT2ksWZh-N9A0SBZ8dysIjoIDSM0qE",
    authDomain: "biga-carona-universidades.firebaseapp.com",
    databaseURL: "https://biga-carona-universidades.firebaseio.com",
    projectId: "biga-carona-universidades",
    storageBucket: "biga-carona-universidades.appspot.com",
    messagingSenderId: "738822352077"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
